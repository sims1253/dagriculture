% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_tables.R
\name{dagri_nodes_df}
\alias{dagri_nodes_df}
\title{Tabular view of graph nodes}
\usage{
dagri_nodes_df(graph)
}
\arguments{
\item{graph}{A \code{dagri_graph}.}
}
\value{
A base \code{data.frame} with one row per node and these columns, in
this exact order:
\itemize{
\item \code{id}, \code{kind}, \code{state}, \code{block_reason}: character.
\item \code{label}: character; \code{NA_character_} when the node has no label.
\item \code{params}, \code{metadata}: list-columns; each cell is the node's
stored \code{params}/\code{metadata} list, verbatim.
}
Node ids live in the \code{id} column as character values and the frame
carries plain automatic row names — ids never become row names. An
empty graph returns a zero-row frame with identical column names and
per-column classes.
}
\description{
Builds a base \code{data.frame} with one row per node, for reporting and quick
inspection. Rows follow graph insertion order (the order of the
\code{graph$nodes} map); sort or subset the result explicitly when a different
order is needed.
}
\details{
Aborts with class \code{dagri_error_invalid_argument} when \code{graph} is not a
valid \code{dagri_graph}.

\code{state} and \code{block_reason} mirror the STORED node fields —
whatever \code{\link[=dagri_recompute_state]{dagri_recompute_state()}} last wrote (\code{"new"} / \code{"none"} until
then). This is an accessor, not a planner: it never derives state.
}
\examples{
registry <- dagri_registry(dagri_kind("source"), dagri_kind("fit"))
graph <- dagri_graph(registry) |>
  dagri_add_node("data", "source", label = "Data") |>
  dagri_add_node("fit", "fit", params = list(method = "lm")) |>
  dagri_add_edge("data", "fit", id = "e1")

nodes <- dagri_nodes_df(graph)
nodes[, c("id", "kind", "label", "state")]
nodes$params
}
