% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_boundary.R
\name{dagri_order_edges}
\alias{dagri_order_edges}
\title{Order edges deterministically by edge id}
\usage{
dagri_order_edges(edges)
}
\arguments{
\item{edges}{A named or unnamed list of edge objects.}
}
\value{
The same list, reordered by edge id.
}
\description{
Returns a copy of \code{edges} sorted by the embedded \code{edge$id} field (falling
back to \code{""} when an edge has no \code{id}). Empty or length-1 lists are returned
unchanged. Container names are preserved. Used by consumers that need a
stable fingerprint of multi-input nodes.
}
\examples{
edges <- list(
  late = list(id = "edge_z", from = "a", to = "b"),
  early = list(id = "edge_a", from = "c", to = "d"),
  middle = list(id = "edge_m", from = "e", to = "f")
)
ordered <- dagri_order_edges(edges)
vapply(ordered, function(e) e$id, character(1))
}
