% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_editing.R
\name{dagri_update_edge}
\alias{dagri_update_edge}
\title{Update an edge in a dagriculture graph}
\usage{
dagri_update_edge(graph, edge_id, type = NULL, metadata = NULL)
}
\arguments{
\item{graph}{A \code{dagri_graph}.}

\item{edge_id}{Edge ID.}

\item{type}{Edge type. When non-\code{NULL}, must be a single non-empty string
and \strong{replaces} the existing \code{type} (see Details).}

\item{metadata}{Edge metadata. When non-\code{NULL}, \strong{replaces} the existing
\code{metadata} outright (see Details); must be a named list of plain data
(closures, environments, formulas, and external pointers are rejected
recursively).}
}
\value{
A new \code{dagri_graph} with the edge updated and \code{version} bumped by 1
(the bump happens even when both field arguments are \code{NULL}); the input
graph is not modified. \code{from}/\code{to} cannot be changed — remove and re-add
the edge to rewire it.
}
\description{
Update an edge in a dagriculture graph
}
\details{
\code{metadata} (when non-\code{NULL}) \strong{replaces} the edge's \code{metadata}
outright; it is NOT merged into the existing metadata. To merge partial
updates, do it in the caller, e.g.
\code{dagri_update_edge(graph, edge_id, metadata = utils::modifyList(old_metadata, new_metadata))}.
A \code{NULL} \code{type}/\code{metadata} leaves that field untouched. This mirrors
\code{dagri_update_node()}'s replace-not-merge primitive: consumers must opt
into merge explicitly instead of silently having a partial update destroy
sibling fields.

Known boundary: \code{dagri_graph_diff()} is a structural id diff — it reports
only added/removed node and edge ids, so metadata and \code{type} edits made
here do not surface in its output.
}
\section{Errors}{

\itemize{
\item \code{dagri_error_invalid_argument} when \code{graph} is malformed, \code{edge_id} is
not a single non-empty string, \code{type} is not a single non-empty string,
or \code{metadata} is not a named list of plain data.
\item \code{dagri_error_not_found} when \code{edge_id} is not an edge in the graph.
}
}

\examples{
reg <- dagri_registry(dagri_kind("source"), dagri_kind("process"))
g <- dagri_graph(reg) |>
  dagri_add_node("raw", "source") |>
  dagri_add_node("fit", "process") |>
  dagri_add_edge("raw", "fit", id = "e1", metadata = list(weight = 1))

g2 <- dagri_update_edge(g, "e1", type = "control", metadata = list(weight = 2))
g2$edges[["e1"]]$type
g2$edges[["e1"]]$metadata
}
