% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_tables.R
\name{dagri_edges_df}
\alias{dagri_edges_df}
\title{Tabular view of graph edges}
\usage{
dagri_edges_df(graph)
}
\arguments{
\item{graph}{A \code{dagri_graph}.}
}
\value{
A base \code{data.frame} with one row per edge and these columns, in
this exact order:
\itemize{
\item \code{id}, \code{from}, \code{to}, \code{type}: character.
\item \code{metadata}: list-column; each cell is the edge's stored
\code{metadata} list, verbatim.
}
Edge ids live in the \code{id} column as character values and the frame
carries plain automatic row names — ids never become row names. An
empty graph returns a zero-row frame with identical column names and
per-column classes.
}
\description{
Builds a base \code{data.frame} with one row per edge, for reporting and quick
inspection. Rows follow graph insertion order (the order of the
\code{graph$edges} map); sort or subset the result explicitly when a different
order is needed.
}
\details{
Aborts with class \code{dagri_error_invalid_argument} when \code{graph} is not a
valid \code{dagri_graph}.
}
\examples{
registry <- dagri_registry(dagri_kind("source"), dagri_kind("fit"))
graph <- dagri_graph(registry) |>
  dagri_add_node("data", "source") |>
  dagri_add_node("fit", "fit") |>
  dagri_add_edge("data", "fit", id = "e1", metadata = list(weight = 2))

edges <- dagri_edges_df(graph)
edges[, c("id", "from", "to", "type")]
edges$metadata
}
