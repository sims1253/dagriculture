% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_boundary.R
\name{dagri_edge_ids}
\alias{dagri_edge_ids}
\title{Sorted unique edge ids}
\usage{
dagri_edge_ids(edges)
}
\arguments{
\item{edges}{A named or unnamed list of edge objects.}
}
\value{
Sorted, de-duplicated character vector of edge ids (possibly empty).
}
\description{
Extracts edge ids from a list of edge objects. Prefers container
\code{names(edges)} when every name is non-empty; otherwise falls back to the
embedded \code{edge$id} field. This dual path keeps the helper usable both for
the canonical named-map storage shape and for unnamed edge lists carrying
embedded ids (for example after \code{unname()}).
}
\details{
Aborts with \code{dagri_error_invalid_argument} when neither path yields complete
non-empty ids, since unidentifiable edges cannot be diffed.
}
\examples{
edges <- list(
  e2 = list(id = "e2", from = "a", to = "b"),
  e1 = list(id = "e1", from = "c", to = "d")
)
dagri_edge_ids(edges)
}
