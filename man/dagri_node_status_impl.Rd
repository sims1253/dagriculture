% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_state.R
\name{dagri_node_status_impl}
\alias{dagri_node_status_impl}
\title{Build the per-node status view for a plan}
\usage{
dagri_node_status_impl(graph, topo_order, external_blocked, index)
}
\arguments{
\item{graph}{A \code{dagri_graph} with derived states from
\code{\link[=dagri_recompute_state_impl]{dagri_recompute_state_impl()}}.}

\item{topo_order}{Character vector of node ids in topological order (the
plan's target closure).}

\item{external_blocked}{Named list from \code{\link[=dagri_external_blocked]{dagri_external_blocked()}}.}

\item{index}{Adjacency index from \code{\link[=dagri_adjacency]{dagri_adjacency()}}.}
}
\value{
A named list keyed by node id, in \code{topo_order} order; each entry is
a named list with \code{state}, \code{block_reason}, \code{pending_gates},
\code{external_hold}, \code{upstream_blockers}, and \code{eligible}.
}
\description{
Pure worker shared by \code{\link[=dagri_plan]{dagri_plan()}}: assembles the per-node \code{node_status}
map — one entry per node in \code{topo_order}, each carrying the derived
structural state, its block reason, the pending gates on the node's inbound
edges, the propagated external hold reason, the direct structurally blocked
upstream neighbors, and final structural eligibility.
}
\details{
Structural eligibility and external blocking stay separate: \code{eligible} is
\code{TRUE} whenever the derived state is "ready", even when the node carries an
\code{external_hold} (mirroring the deliberate overlap between \code{plan$eligible}
and \code{plan$external_blocked}).

Deterministic ordering: entries follow \code{topo_order}; \code{pending_gates} follows
incoming-edge insertion order, then gate insertion order within each edge;
\code{upstream_blockers} follows incoming-edge insertion order and is unique.
}
\keyword{internal}
