% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_tables.R
\name{dagri_table_list}
\alias{dagri_table_list}
\title{List-column from record fields}
\usage{
dagri_table_list(records, field)
}
\arguments{
\item{records}{Named list of records (nodes, edges, or gates).}

\item{field}{Name of the list field to extract.}
}
\value{
Unnamed list with one cell per record, in record (insertion)
order.
}
\description{
Internal helper for the tabular accessors. Extracts \code{field} from every
record in \code{records}, verbatim, as one cell per row. Graphs store these
fields as plain lists, so the cells are plain lists; no graph or record
objects leak into cells. An empty record list yields the empty \code{list()}
column of the empty-graph skeleton.
}
\keyword{internal}
