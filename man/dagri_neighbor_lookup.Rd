% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_queries.R
\name{dagri_neighbor_lookup}
\alias{dagri_neighbor_lookup}
\title{Resolve a node's immediate neighbors}
\usage{
dagri_neighbor_lookup(graph, node_id, neighbor_fn, index = NULL)
}
\arguments{
\item{graph}{A \code{dagri_graph}.}

\item{node_id}{Node ID.}

\item{neighbor_fn}{Neighbor selector: \code{dagri_upstream} or \code{dagri_downstream}.}

\item{index}{Optional pre-built adjacency index from \code{\link[=dagri_adjacency]{dagri_adjacency()}}.}
}
\value{
Character vector of neighbor node ids.
}
\description{
Internal helper used by \code{\link[=dagri_dfs]{dagri_dfs()}}. When a pre-built \code{index} is supplied,
it returns the relevant map directly (O(1)); otherwise it falls back to a
linear scan over the edge list for compatibility. For a custom
\code{neighbor_fn}, \code{index} is ignored and \code{neighbor_fn} is called.
}
\keyword{internal}
