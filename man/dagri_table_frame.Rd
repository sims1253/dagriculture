% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_tables.R
\name{dagri_table_frame}
\alias{dagri_table_frame}
\title{Assemble a tabular accessor's data.frame}
\usage{
dagri_table_frame(scalar_columns, list_columns)
}
\arguments{
\item{scalar_columns}{Named list of equal-length character vectors.}

\item{list_columns}{Named list of list-columns (one cell per row).}
}
\value{
A base \code{data.frame} with the scalar columns followed by the
list-columns.
}
\description{
Internal helper shared by \code{\link[=dagri_nodes_df]{dagri_nodes_df()}}, \code{\link[=dagri_edges_df]{dagri_edges_df()}}, and
\code{\link[=dagri_gates_df]{dagri_gates_df()}}. The scalar character columns go through
\code{data.frame()}; the list-columns are attached afterwards because
\code{data.frame()} would spread a list argument across several columns.
Row names stay the plain automatic sequence — record ids are data in the
\code{id} column, never row names. When every column is zero-length this
returns the empty skeleton: a zero-row frame with identical column names
and per-column classes as the populated case.
}
\keyword{internal}
