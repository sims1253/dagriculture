% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_boundary.R
\name{dagri_diff_records}
\alias{dagri_diff_records}
\title{Diff shared records field by field}
\usage{
dagri_diff_records(before_map, after_map, common_ids, fields, include_values)
}
\arguments{
\item{before_map}{Named list of before-state records keyed by id.}

\item{after_map}{Named list of after-state records keyed by id.}

\item{common_ids}{Character vector of ids present in both maps.}

\item{fields}{Character vector of field names to compare.}

\item{include_values}{Whether to collect before/after values per change.}
}
\value{
A list with \code{changed_ids} (character vector in \code{common_ids} order)
and \code{values} (named list keyed by changed ids; each entry a named list of
only the differing fields, each \verb{list(before = <value>, after = <value>)}; an empty named list when \code{include_values} is \code{FALSE}).
}
\description{
Internal worker for \code{\link[=dagri_graph_diff]{dagri_graph_diff()}}: walks \code{common_ids} (ids present in
both graphs, already ordered by the caller) and compares the tracked
\code{fields} per record with \code{\link[=identical]{identical()}} on the stored values, so \code{NULL} vs
\code{NULL} compares equal.
}
\keyword{internal}
