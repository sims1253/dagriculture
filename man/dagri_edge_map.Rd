% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_boundary.R
\name{dagri_edge_map}
\alias{dagri_edge_map}
\title{Key an edge collection by id, preserving storage order}
\usage{
dagri_edge_map(edges)
}
\arguments{
\item{edges}{A named or unnamed list of edge objects.}
}
\value{
A named list of edge objects keyed by edge id (possibly empty).
}
\description{
Internal building block for the graph diff: returns the edge objects as a
named list keyed by edge id in storage (insertion) order. Follows the same
dual path as \code{\link[=dagri_edge_ids]{dagri_edge_ids()}} — container \code{names(edges)} when every name
is non-empty, otherwise the embedded \code{edge$id} fields — so both the
canonical named-map storage shape and unnamed edge lists with embedded ids
are supported.
}
\details{
Aborts with \code{dagri_error_invalid_argument} when neither path yields
complete non-empty ids, since unidentifiable edges cannot be diffed.
}
\keyword{internal}
