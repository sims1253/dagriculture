% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_boundary.R
\name{dagri_incoming_edges}
\alias{dagri_incoming_edges}
\title{Incoming edges for a node}
\usage{
dagri_incoming_edges(graph, node_id)
}
\arguments{
\item{graph}{A \code{dagri_graph}.}

\item{node_id}{Single character string naming a node in \code{graph}.}
}
\value{
A named list of edge objects (possibly empty).
}
\description{
Returns the edge objects whose \code{to} endpoint is \code{node_id}, preserving the
container names of \code{graph$edges}. Unlike \code{dagri_upstream()}, which returns
neighbor node ids, this returns the full edge objects so callers can inspect
edge ids, types, and metadata.
}
\examples{
graph <- dagri_graph(dagri_registry(dagri_kind("source"), dagri_kind("fit")))
graph <- dagri_add_node(graph, "data", "source")
graph <- dagri_add_node(graph, "fit", "fit")
graph <- dagri_add_edge(graph, "data", "fit", id = "e1")
incoming <- dagri_incoming_edges(graph, "fit")
length(incoming) == 1
}
