# Add a gate to a groots graph

Add a gate to a groots graph

## Usage

``` r
groots_add_gate(graph, edge_id, id = NULL, metadata = list())
```

## Arguments

- graph:

  A `groots_graph`.

- edge_id:

  Edge ID.

- id:

  Optional Gate ID.

- metadata:

  Gate metadata.
