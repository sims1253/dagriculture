# Get upstream nodes

Returns the unique node ids with an edge into `node_id`. O(E).

## Usage

``` r
dagri_upstream(graph, node_id)
```

## Arguments

- graph:

  A `dagri_graph`.

- node_id:

  Node ID.
