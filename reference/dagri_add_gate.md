# Add a gate to a dagriculture graph

Add a gate to a dagriculture graph

## Usage

``` r
dagri_add_gate(graph, edge, id = NULL, metadata = list())
```

## Arguments

- graph:

  A `dagri_graph`.

- edge:

  Edge ID.

- id:

  Optional Gate ID.

- metadata:

  Gate metadata.
