# Get terminal nodes

Get terminal nodes

## Usage

``` r
dagri_terminal(graph, targets = NULL)
```

## Arguments

- graph:

  A `dagri_graph`.

- targets:

  Optional target nodes.

## Details

O(V+E).
