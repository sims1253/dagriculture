# Add a node to a groots graph

Add a node to a groots graph

## Usage

``` r
groots_add_node(
  graph,
  id,
  kind,
  label = NULL,
  params = list(),
  metadata = list()
)
```

## Arguments

- graph:

  A `groots_graph`.

- id:

  Node ID.

- kind:

  Node kind.

- label:

  Node label.

- params:

  Node parameters.

- metadata:

  Node metadata.
